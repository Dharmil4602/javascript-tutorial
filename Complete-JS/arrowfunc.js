const greet = () => `Good Morning Dharmil`;
console.log(greet());

// Using Arrow Function For Object.

const greet1 = () => ({name: `Dharmil`}); // In one liner we need to include object inside Ciruclar braces.
console.log(greet1());